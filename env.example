## Backend (Next.js API) - Example Environment Variables
## Copy to `ansh-apparels-backend/.env.local` and fill real values (do NOT commit `.env.local`)

# comma-separated list of allowed frontend origins (must match browser Origin EXACTLY)
# Example (dev):
# CORS_ORIGINS=http://localhost:5005,http://127.0.0.1:5005
# If you open the site via LAN IP (shown by Next dev), also add it:
# CORS_ORIGINS=http://localhost:5005,http://192.168.1.199:5005
CORS_ORIGINS=http://localhost:5005

# comma-separated list of admin emails that can access /api/admin/*
ADMIN_EMAILS=admin@example.com

# Optional: invite code to create an admin via signup (server-side secret)
ADMIN_INVITE_CODE=change-me-to-a-random-secret

# JWT secret for signing access tokens (7 days expiry)
JWT_SECRET=change-me-to-a-long-random-secret

# MongoDB
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_DB_NAME=ansh_apparels


